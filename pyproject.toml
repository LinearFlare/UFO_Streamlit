[project]
name = "ufo-sklearn"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
# 修改此处：锁定为 Python 3.11 版本段（允许 3.11.9 及以上的 3.11 补丁包）
requires-python = ">=3.11,<3.12" 

dependencies = [
    "fastapi>=0.110.0",
    "pandas>=2.2.0",        # 调整：2.2.x 是 Python 3.11 最稳定的黄金组合
    "requests>=2.31.0",
    "scikit-learn>=1.4.0",  # 调整：1.4+ 完美适配 Python 3.11
    "streamlit>=1.35.0",
    "uvicorn[standard]>=0.29.0",
]


[tool.poetry]
package-mode = false